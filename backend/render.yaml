services:
  - type: web
    name: personascape-api
    runtime: node
    buildCommand: ./deploy.sh
    startCommand: npm run deploy:js
    healthCheckPath: /api/test-cors
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false 